---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getBlogPosts } from '../../lib/posts';
import { PUBLIC_SITE_URL, SITE_DESCRIPTION, SITE_NAME } from '../../lib/site';

const posts = await getBlogPosts();
const canonical = new URL('/stories', PUBLIC_SITE_URL).href;
const collectionJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  name: `Stories | ${SITE_NAME}`,
  url: canonical,
  description: SITE_DESCRIPTION,
};
---

<BaseLayout
  title="Stories | Team Chords"
  description="Launch announcements, product stories, and updates from Team Chords."
  canonical={canonical}
  image="/hero-story.svg"
  imageAlt="Team Chords stories hero artwork"
  jsonLd={collectionJsonLd}
>
  <section class="mx-auto max-w-6xl px-4 pb-10 sm:px-6 lg:px-8">
    <div class="rounded-[2rem] bg-gray-700 px-6 py-12 text-white shadow-xl shadow-gray-900/10 sm:px-8 sm:py-14">
      <p class="text-xs font-semibold uppercase tracking-[0.2em] text-gray-300">Stories</p>
      <h1 class="mt-3 max-w-3xl text-4xl font-bold tracking-tight sm:text-5xl">Launch stories and product notes</h1>
      <p class="mt-4 max-w-3xl text-lg leading-8 text-gray-300">Start with the launch announcement, then read the stories behind the product and the workflow problems Team Chords is built to solve.</p>
    </div>
  </section>

  <section class="mx-auto max-w-6xl px-4 pb-16 sm:px-6 lg:px-8">
    <div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">
      {posts.map((post) => (
        <a class="group rounded-2xl border border-gray-200 bg-white p-5 shadow-sm shadow-gray-900/5 transition hover:-translate-y-1 hover:shadow-lg hover:shadow-gray-900/10" href={`/stories/${post.slug}`}>
          <div class="flex flex-wrap items-center gap-2 text-xs font-semibold uppercase tracking-[0.2em] text-gray-500">
            <span class="rounded-full bg-gray-100 px-3 py-1 text-gray-700">{post.featured ? 'Featured' : 'Article'}</span>
            <span>{post.pubDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}</span>
          </div>
          <h2 class="mt-4 text-2xl font-semibold tracking-tight text-gray-900 transition group-hover:text-gray-700">{post.title}</h2>
          <p class="mt-3 text-sm leading-7 text-gray-600">{post.description}</p>
          <div class="mt-4 flex flex-wrap gap-2">
            {post.tags.map((tag) => <span class="rounded-full border border-gray-200 bg-gray-50 px-3 py-1 text-xs font-medium text-gray-600">{tag}</span>)}
          </div>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>
